<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Menu item for navigating back to the main menu -->
    <item
        android:id="@+id/menu_back_to_main"
        android:title="@string/menu_back_to_main"
        tools:ignore="HardcodedText" />

    <!-- Additional items for parking budget simulator and reviews -->
    <item
        android:id="@+id/menu_parking_budget_simulator"
        android:title="@string/menu_parking_budget_simulator"
        tools:ignore="HardcodedText" /> <!-- Accessible for all users -->

    <item
        android:id="@+id/menu_notifications"
        android:title="@string/menu_notifications"
        tools:ignore="HardcodedText" /> <!-- Only for logged-in users -->

    <item
        android:id="@+id/menu_chat"
        android:title="@string/menu_chat"
        tools:ignore="HardcodedText" /> <!-- Only for logged-in users -->

    <item
        android:id="@+id/menu_submit_petition"
        android:title="@string/menu_submit_petition"
        tools:ignore="HardcodedText" /> <!-- Only for logged-in users -->

    <item
        android:id="@+id/menu_submit_review"
        android:title="@string/menu_submit_review"
        tools:ignore="HardcodedText" /> <!-- Only for logged-in users -->

    <item
        android:id="@+id/menu_take_survey"
        android:title="@string/menu_take_survey"
        tools:ignore="HardcodedText" /> <!-- Accessible for all users -->

</menu>