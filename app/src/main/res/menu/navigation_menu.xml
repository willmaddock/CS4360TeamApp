<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Menu item for continuing as a guest -->
    <item
        android:id="@+id/menu_continue_as_guest"
        android:title="Continue as Guest"
        tools:ignore="HardcodedText" />

    <!-- Menu item for logging out -->
    <item
        android:id="@+id/menu_logout"
        android:title="Log Out"
        tools:ignore="HardcodedText" />

    <!-- Menu item for navigating back to the main menu -->
    <item
        android:id="@+id/menu_back_to_main"
        android:title="Back to Main Menu"
        tools:ignore="HardcodedText" />

    <!-- Additional items for payment and reviews -->
    <item
        android:id="@+id/menu_pay_parking_meter"
        android:title="Pay Parking Meter"
        tools:ignore="HardcodedText" /> <!-- Accessible for both users -->

    <item
        android:id="@+id/menu_notifications"
        android:title="Notifications"
        tools:ignore="HardcodedText" /> <!-- Only for logged-in users -->

    <item
        android:id="@+id/menu_chat"
        android:title="Chat"
        tools:ignore="HardcodedText" /> <!-- Only for logged-in users -->

    <item
        android:id="@+id/menu_submit_petition"
        android:title="Submit a Petition"
        tools:ignore="HardcodedText" /> <!-- Only for logged-in users -->

    <item
        android:id="@+id/menu_submit_review"
        android:title="Submit a Review"
        tools:ignore="HardcodedText" /> <!-- Only for logged-in users -->

    <item
        android:id="@+id/menu_take_survey"
        android:title="Take a Survey"
        tools:ignore="HardcodedText" /> <!-- Only for guests -->
</menu>